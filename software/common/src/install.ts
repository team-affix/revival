export function apm_install() {
    // Installs agda packages into the current working directory, given the directory is a apm venv

    // // Look for subfolder with same name as the current working directory
    // const cwd = process.cwd();
    // const venvName = path.basename(cwd);
    // const rootPackagePath = path.join(cwd, venvName);


}
